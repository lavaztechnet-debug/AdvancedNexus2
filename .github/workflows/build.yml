- name: Set up Java Development Kit (JDK) 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
        build-path: 'your-subfolder-name' # Change this to your subfolder if applicable
